context:
  version: "3.7"

package:
  name: tmux
  version: ${{ version }}_

source:
  url: https://github.com/tmux/tmux/archive/${{ version }}.tar.gz
  sha256: 123b9ed37c7ee957327a483024ea6ae0e6032348c02680be2ac91b9350e62d36

build:
  number: 0
  skip:
    - win

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - autoconf
    - automake
    - libtool
    - m4
    - make
    - patch
    - pkg-config
    - bison
    - if: unix
      then:
        - gnuconfig
  host:
    - libevent
    - ncurses
    - if: osx
      then:
        - libutf8proc
  run:
    - libevent
    - ncurses

tests:
  - script:
      - tmux -V

about:
  homepage: https://tmux.github.io/
  license: ISC
  license_file: COPYING
  summary: A terminal multiplexer.

extra:
  recipe-maintainers:
    - XhstormR
    - jakirkham
    - djsutherland
    - dbast
    - matthiasdiener
    - seowalex
